def input_fn(input:dict,param={}) -> dict:
    """
    Return the input dictionary as is.

    Args:
        input: A dictionary to be returned.

    Returns:
        The input dictionary unchanged.
    """
    return input

